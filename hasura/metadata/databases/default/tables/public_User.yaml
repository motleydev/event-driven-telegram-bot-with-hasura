table:
  name: user
  schema: public
array_relationships:
  - name: reports
    using:
      foreign_key_constraint_on:
        column: t_user_id
        table:
          name: report
          schema: public
